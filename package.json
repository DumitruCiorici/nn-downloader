{
  "name": "nn-converter",
  "version": "1.0.0",
  "scripts": {
    "start": "node server/server.js",
    "build": "cd client && npm install && npm run build",
    "deploy": "vercel deploy --prod"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "ytdl-core": "^4.11.4"
  },
  "devDependencies": {
    "vercel": "^24.0.0"
  }
} 